# herdr-sessions has no runtime .env -- per-host config lives in profiles/*.conf
# (HERDR_BIN, RESUME_MODE, FALLBACK, ... -- see README.md and the profile files
# themselves). The one thing systemd sets as an actual environment variable is:
HERDR_PROFILE=profiles/example.conf
